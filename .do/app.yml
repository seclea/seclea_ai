name: seclea-ai
region: ams
static_sites:
- environment_slug: html
  github:
    branch: dev
      : true
    repo: seclea/seclea_ai
  name: seclea-ai
  routes:
  - path: /
  source_dir: /docs/build/html/